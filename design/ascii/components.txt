-------------------------------------------------------------------------------
DIRECTIONS FOR AGENTS CONSUMING THIS FILE
-------------------------------------------------------------------------------
This file is the AskeeDS ASCII design system and component library (canonical
location: design/ascii/components.txt). By Spencer Goldade. Read it when
implementing or modifying terminal UI, layouts, panes, or text-based components
for any TUI using AskeeDS.

HOW TO PARSE
  - Component start: line beginning with three U+241F (␟) then "COMPONENT: "
    followed by the component name (e.g. layout.app.shell, room-card.default).
  - Meta: lines beginning with one ␟ then a key and value (e.g. ␟ props: ...,
    ␟ description: ...). Meta applies to the component that follows the
    previous component boundary.
  - Everything after the meta block, until the next ␟␟␟ line, is the
    component's ASCII art (structure and example content). A single blank line
    between the last meta line and the first art line is recommended for
    readability and is treated as part of the art block.
  - Do not use the character ␟ (U+241F) inside ASCII art. Parsers split on ␟
    to find component boundaries and meta lines; a ␟ in the art would break
    parsing. Use only box-drawing and content characters in the art block.

HOW TO USE
  - Look up a component by name when building or styling a TUI element. Use
    the ASCII art as the structural reference (borders, spacing, layout).
  - Border characters (| - + etc.) are defined in design/ascii/box-drawing.yaml;
    load from there in code rather than hardcoding. This file shows the
    intended shape only.
  - Semantic colors are in design/tokens/colors.md. Apply them in code
    (Rich/Textual) per ␟ color-hint: on each component when present.
  - Respect props listed in meta (e.g. title, description_text, directions[])
    when implementing; they define the data the component expects.
  - Max width 80 chars; prefer 60–70 for body text. Test at 80×24; 256-color.
  - Layout components (e.g. layout.two-column, layout.stack): Props like
    left_content, right_content, and blocks[] are content slots. Your runtime
    may pass pre-rendered strings or the output of other components; this file
    does not define a composition API. Ensure what you pass respects max width
    (80 chars) and box-drawing.

HOW TO ADD A NEW COMPONENT
  - Pick a name:
    - Use dot notation (category.variant), e.g. layout.app.shell, menu.main.
    - Keep it stable; avoid reusing the same name for different layouts.
  - Add a component block:
    - Start with a boundary line: ␟␟␟ COMPONENT: <name>
    - Add meta lines: at minimum ␟ description: and ␟ props:
    - Optionally add ␟ shape:, ␟ variant:, ␟ interactive:, ␟ color-hint:
  - Sketch the ASCII art:
    - Use only box characters from box-drawing.yaml for borders.
    - Stay within 80 characters per line; prefer 60–70 for readability.
    - Do not use the ␟ character anywhere in the art block.
  - Update contracts:
    - If you introduce new shape patterns, reflect them in PROP SHAPES below.
    - Update design/docs (e.g. ascii-design-system.md) only if the component
      changes higher-level flows or concepts.
  - Keep manifest in sync:
    - Run `python3 tools/update_manifest.py` (or `npm run update:manifest`) to
      regenerate design/ascii/manifest.yaml from components.txt so tools and
      tests can verify coverage.

PROPS CONTRACT
  - Naming: Prop names use snake_case (e.g. hp_current, body_text). Refer to
    props by name (e.g. props["title"], keyword arguments, or your runtime's
    equivalent).
  - Optionality: If a prop is optional, the component's ␟ props line may suffix
    it with _optional or list optional props on a separate ␟ optional: line.
    Props without that marking are required unless the description says otherwise.
  - Arrays: A prop named with trailing [] (e.g. items[]) denotes a list. The
    shape of each element is defined in PROP SHAPES below or in the component's
    ␟ shape: line.
  - Single source of truth: When implementing, treat the component's ␟ props:
    and any ␟ shape: (or PROP SHAPES entry) as the contract for what to pass in.

PROP SHAPES
  - room-card.default: entity_list: { id, label }[] (items/NPCs in room).
    exits: string[] (e.g. ["north","south"]).
  - entity-list.room: items: { id, label }[], npcs: { id, label }[].
  - minimap.default: grid: 2D array of role ids or characters (per map-tiles.yaml).
    legend_entries: { char, label }[]. player_position: { row, col } or equivalent.
  - layout.two-column: left_content, right_content: pre-rendered string or
    runtime's child content (see composition note in HOW TO USE).
  - layout.stack: blocks[]: list of pre-rendered strings or child content.
  - tracker.objective: objectives: { id?, label, checked: bool }[].
  - inventory.grid: slots: { id, label?, item_ref? }[]. columns: number.
  - inventory.list: items: { id, label, quantity? }[].
  - character-sheet.compact: stats: { label, value }[] or { label, current, max }[]
    for bar-style rows.
  - tree.compact: nodes: { id, label, children?: nodes }[] (nested) or
    { id, label, depth }[] (flat with indent).
  - choice-wheel.inline: options: { id, label }[].
  - quick-select.radial: options: { id, label }[]. selected_id: id of selected.
    Positions 1-4 (or 1-8) map clockwise from top: 1=top, 2=right, 3=bottom, 4=left.
  - nav.vertical: items: { id, label }[]. active_id: id of the active item.
  - menu.main / menu.pause: items: { id, label }[].
  - status-icon.row: icons: { symbol, label? }[] or { id, symbol, label? }[].
  - screen.death: actions: { id, label }[] (e.g. Respawn, Main menu).
  - decoration.placeholder: width, height: character-cell dimensions of the slot;
    art is cropped or centered to fit. Content is a separate asset or catalog entry.
  - panel.survival-status: needs: { label, current, max }[].
  - tree.relationships: relations: { name, attitude, tags: string[] }[].
  - screen.crafting: inputs: { label, item_name?, quantity? }[]; resource_costs:
    { label, amount }[].
  - cooldown.row: abilities: { label, turns_left }[].
  - panel.consequence: body_conditions: { label, severity }[]; mental_conditions:
    { label, severity }[].

COLOR-HINT BEHAVIOR
  - HP / resource bands (status.good, status.warning, status.danger): Use good
    when value is above ~70% of max, warning between ~30% and ~70%, danger below
    ~30%. Exact thresholds are implementation-defined; document the intent.
  - "Entity for others" (e.g. mana, stamina): Use tokens from design/tokens/
    colors.md for non-HP resources (e.g. entity.item, ui.accent) as appropriate;
    do not rely on color alone for meaning.

RELATED SOURCES
  - design/ascii/README.md, box-drawing.yaml, map-tiles.yaml, manifest.yaml
  - design/tokens/colors.md (semantic color tokens)
  - docs/ascii-design-system.md (overview)

-------------------------------------------------------------------------------
GUIDELINES FOR ASCII DESIGN SYSTEM COMPONENTS (authors / editors)
-------------------------------------------------------------------------------
Source of truth: design/ascii/ (this file, box-drawing.yaml, map-tiles.yaml,

PATTERNS AND USAGE
  - Prefer composing components (e.g. modal = overlay + card + buttons). Use
    ␟ pattern: / ␟ use-case: to link components to flows.
  - Variants: When a component lists ␟ variant: (e.g. tooltip.default with
    default | stats) or when two components share the same structure (e.g.
    menu.main and menu.pause), implement as one parameterized component when
    the layout is identical; use the variant or use-case to change props or
    copy only. Document which props apply to which variant in the component's
    ␟ props: / ␟ optional: lines.

ACCESSIBILITY AND CONTENT
  - Structure (headings, lists) supports screen readers; keep order and labels
    consistent. Color is applied in code per design/tokens/colors.md; do not
    rely on shape alone for meaning.
  - Content style: Short, present tense; noun-based labels. Match
    design/content/voice-and-tone.md when adding copy examples.
  - Brand: One consistent box-drawing set (box-drawing.yaml); avoid mixing
    decorative Unicode in structure. Decorative ASCII art in a dedicated slot
    (decoration.placeholder) uses ASCII-only characters.
  - Empty and error states: When a component has no data (e.g. empty inventory,
    no objectives, failed load), show a single-line placeholder message or the
    component structure with one line such as "(none)" or "No items." Avoid
    leaving the component area blank. For errors, use feedback.error or a
    dedicated line with status.danger.

DELIMITERS
  - ␟␟␟ COMPONENT: <name>   Start of a new component (U+241F SYMBOL FOR UNIT SEPARATOR).
  - ␟ <key>: <value>         Meta line: description, props, variants, etc.
  One ␟ = meta; three ␟ = component boundary. No other uses of ␟ in art.

NAMING
  - Use dot notation: category.variant (e.g. layout.app.shell, button.icon).
  - Noun-based, present tense; avoid jargon. Matches design tokens (e.g.
    status.good, entity.item).

BOX-DRAWING AND WIDTH
  - Use characters from design/ascii/box-drawing.yaml for borders. Do not
    hardcode alternate box glyphs here; change the YAML and keep this file
    consistent with the active set (e.g. light: | - +).
  - Max line length 80 chars; aim 60–70 for readable blocks (per ui-ux §5.1).
  - Test at 80×24 min; 256-color safe. No truecolor-only or wrapping.

SPACING AND HIERARCHY
  - One space between border and inner text (padding). Keep vertical
    rhythm consistent (blank line between sections when the component has
    distinct regions). Keep primary vs secondary visually distinct.

PROPS AND META
  - List props on ␟ lines: ␟ props: name, label, icon. Optional: ␟ variant:,
    ␟ description:, ␟ shape:. Code will use these when implementing components.
  - Interactivity: Optional meta ␟ interactive: true indicates that the
    component is focusable or emits events (e.g. button, menu item, choice
    option). Use for accessibility and focus order; this file does not define
    event names or key bindings.

COLOR
  - This file defines structure only. Semantic color lives in
    design/tokens/colors.md and is applied in code (Rich/Textual). You may
    add ␟ color-hint: <token> for documentation (e.g. ␟ color-hint: ui.accent
    for border).

ADDING COMPONENTS
  - Check existing components to avoid duplicates. Use the same ␟/␟␟␟
    convention. Add at least ␟ description: and ␟ props: (if any). Keep
    examples minimal and representative. Add the component name to
    design/ascii/manifest.yaml.

-------------------------------------------------------------------------------
COMPONENT LIBRARY (examples)
-------------------------------------------------------------------------------

␟␟␟ COMPONENT: layout.app.shell
␟ description: Main app shell with header, sidebar, and content area
␟ usage: Use as the top-level frame for the whole TUI, wrapping nav and main content.

+------------------------------------------------+
| App Title                 [user] [settings]    |
+------------+-----------------------------------+
| nav item 1 | content area                      |
| nav item 2 |                                   |
+------------+-----------------------------------+

␟␟␟ COMPONENT: button.icon
␟ description: Button with leading icon (one or two chars).
␟ usage: Use for primary or secondary actions where a small symbol reinforces meaning.
␟ props: label, icon

[☆] Star this

␟␟␟ COMPONENT: tooltip.default
␟ description: Bordered tooltip; optional stats variant.
␟ usage: Show contextual help or quick stats when the user focuses or hovers an element.
␟ props: text; optional variant stats: name, stats_dict
␟ variant: default | stats
␟ shape: box.single

+-------------------------------+
|  Additional information for   |
|  your perusal                 |
+-------------------------------+

␟␟␟ COMPONENT: table.fourcolumn
␟ description: Four-column bordered table; header row and data rows.
␟ usage: Use for structured tabular data such as stats, settings, or comparisons.
␟ props: label, content

+----------------------------------+---------+------------------------+----------------+
|               Col1               |  Col2   |          Col3          | Numeric Column |
+----------------------------------+---------+------------------------+----------------+
| Value 1                          | Value 2 | 123                    |           10.0 |
| Separate                         | cols    | with a tab or 4 spaces |       -2,027.1 |
| This is a row with only one cell |         |                        |                |
+----------------------------------+---------+------------------------+----------------+

␟␟␟ COMPONENT: status-bar.default
␟ description: Single-line HUD; HP, location, turn. Top or bottom of screen.
␟ usage: Pin to the top or bottom of the screen to show core moment-to-moment status.
␟ props: hp_current, hp_max, location_name, turn_count
␟ color-hint: status.good/warning/danger for HP band; ui.muted for labels

HP: 85/100  |  The Clearing  |  Turn 12

␟␟␟ COMPONENT: status-bar.risk
␟ description: Single-line HUD with risk and luck bands in addition to core status.
␟ usage: Swap in when the game tracks global danger level or player fortune alongside HP.
␟ props: hp_current, hp_max, location_name, turn_count, risk_level, luck_band_optional
␟ color-hint: status.good/warning/danger for HP; entity.alert for high risk; ui.accent for luck

HP: 40/100  |  The Alley  |  Turn 27  |  Risk: Hunted  |  Luck: Fading

␟␟␟ COMPONENT: room-card.default
␟ description: Room pane: title, body, then engine-generated "You see" and Exits.
␟ usage: Use as the main descriptive block when the player enters or examines a room.
␟ props: title, description_text, entity_list[], exits[]

+-- The Clearing ---------------------------------+
| A small clearing in the woods.                  |
|                                                 |
| You see:                                        |
|   * brass lamp                                  |
|   * guard                                       |
| Exits: north  south                             |
+-------------------------------------------------+

␟␟␟ COMPONENT: entity-list.room
␟ description: Scannable list of items and NPCs in current room (from content).
␟ usage: Render under a room description or in a sidebar to list interactable entities.
␟ props: items[], npcs[]
␟ color-hint: entity.item (items), entity.npc (NPCs)

You see:
  * brass lamp
  * guard

␟␟␟ COMPONENT: exit-list.inline
␟ description: Exits line derived from room frontmatter exits map.
␟ usage: Place after a room description or entity list to advertise available directions.
␟ props: directions[]
␟ color-hint: entity.npc (exits as links/affordances)

Exits: north  south  east

␟␟␟ COMPONENT: minimap.default
␟ description: Small ASCII grid (e.g. 10x10) with legend. Secondary pane.
␟ usage: Show in a secondary pane to give spatial context without replacing text descriptions.
␟ props: grid[], legend_entries, player_position

+----------+
| ....#....|
| .P.#.....|
| ....D....|
+----------+
  . empty  # wall  P you  D door

␟␟␟ COMPONENT: command-input.default
␟ description: Command line with prompt; optional tab/history hint.
␟ usage: Use at the bottom of the screen as the primary parser or command entry point.
␟ props: prompt, placeholder, hint_text

> go north
[Tab: complete] [Up/Down: history]

␟␟␟ COMPONENT: hint-bar.contextual
␟ description: Contextual shortcuts; progressive disclosure for new vs expert.
␟ usage: Show above or below input to teach core commands without overwhelming experts.
␟ props: hints[]

Commands: look, go <dir>, take <item>, i(nventory), ? for help

␟␟␟ COMPONENT: hint-bar.interactions
␟ description: Context-sensitive interaction hints emphasizing playful or emotional verbs.
␟ usage: Surface fun, optional verbs like pet, talk, or inspect when relevant entities are present.
␟ props: interactions[]

You can: pet dog, talk to guard, inspect statue

␟␟␟ COMPONENT: narrative-log.pane
␟ description: Scrollable narrative output; primary focus area. Bordered pane.
␟ usage: Use as the main scrolling history of events, descriptions, and dialog.
␟ props: lines[], max_visible

+-- Narrative log ---------------------------------+
| You enter the clearing.                          |
| The guard eyes you but says nothing.             |
|                                                  |
+--------------------------------------------------+

␟␟␟ COMPONENT: feedback.success
␟ description: One-line parser success (e.g. take, drop). Green in code.
␟ usage: Display directly under the input or log to confirm accepted commands.
␟ props: message
␟ color-hint: status.good

Taken: rusty sword (+1 weapon)

␟␟␟ COMPONENT: feedback.error
␟ description: One-line parser error or suggestion. Yellow/red in code.
␟ usage: Use when a command fails to parse or act, ideally with a suggested fix.
␟ props: message, suggestion
␟ color-hint: status.warning or status.danger

You don't see "sword" here. Did you mean "rusty sword"?

␟␟␟ COMPONENT: divider.horizontal
␟ description: Section separator within a pane; no box.
␟ usage: Separate logical sections inside a larger pane without drawing a full box.
␟ props: (none or style)

------------------------------------------

---------- Atoms ----------

␟␟␟ COMPONENT: input.text
␟ description: Single-line prompt and field (bordered or prompt-only).
␟ usage: Use for short, focused text entry such as character names or menu filters.
␟ props: prompt, placeholder, max_length

> _______________

␟␟␟ COMPONENT: label.inline
␟ description: Short label before value (e.g. stat display).
␟ usage: Pair with numbers or short values in HUDs, sheets, and summary rows.
␟ props: label, value

HP: 85

␟␟␟ COMPONENT: icon.placeholder
␟ description: Single- or two-char icon slot for buttons/menus.
␟ usage: Reserve space for an icon when you do not yet know the final symbol.
␟ props: symbol, alt_text

[?]

␟␟␟ COMPONENT: button.text
␟ description: Text-only button; no icon.
␟ usage: Use for simple confirm/cancel or secondary actions in modals and screens.
␟ props: label
␟ interactive: true

[ Submit ]

␟␟␟ COMPONENT: breadcrumb.inline
␟ description: Inline path (e.g. location hierarchy).
␟ usage: Show above a main pane to orient the player in nested locations or menus.
␟ props: segments[]

Home > The Clearing > Guard post

---------- Molecules ----------

␟␟␟ COMPONENT: form.single-field
␟ description: Label + input + optional hint in one block.
␟ usage: Use for simple forms like naming a character or entering a single setting.
␟ props: label, placeholder, hint

+----------------------------------+
| Name: _____________________      |
| (your character name)            |
+----------------------------------+

␟␟␟ COMPONENT: card.simple
␟ description: Generic bordered title + body; room-card is game variant.
␟ usage: Use as a generic info card for help, lore snippets, or small summaries.
␟ props: title, body_text
␟ shape: box.card

+-- Card title ------------------------+
| Body text goes here and may wrap     |
| across multiple lines when needed.   |
+--------------------------------------+

---------- Organisms ----------

␟␟␟ COMPONENT: header.banner
␟ description: Single-line app/game title with optional trailing controls.
␟ usage: Place at the very top of layouts to carry branding and global controls.
␟ props: title, controls[]

+------------------------------------------------+
| AskeeDS                   [user] [settings]     |
+------------------------------------------------+

␟␟␟ COMPONENT: nav.vertical
␟ description: Vertical list of nav items (e.g. sidebar).
␟ usage: Use in sidebars or left panes for primary navigation between screens.
␟ props: items[], active_id

| Inventory
| Map
| Journal
> Settings

---------- Templates ----------

␟␟␟ COMPONENT: layout.two-column
␟ description: Left/right panes; no header (use with header.banner if needed).
␟ usage: Split the screen between navigation/secondary content and a main detail pane.
␟ props: left_content, right_content

+------------------+-----------------------------+
| left pane        | right pane                  |
|                  |                             |
+------------------+-----------------------------+

␟␟␟ COMPONENT: layout.stack
␟ description: Stacked full-width blocks (e.g. status + main + input).
␟ usage: Compose vertical flows like status bar, log, and input stacked top to bottom.
␟ props: blocks[]

+------------------------------------------------+
| block 1 (e.g. status bar)                      |
+------------------------------------------------+
| block 2 (main content)                         |
|                                                |
+------------------------------------------------+
| block 3 (input)                                |
+------------------------------------------------+

---------- Patterns (structural) ----------

␟␟␟ COMPONENT: modal.overlay
␟ description: Centered bordered box; pattern for confirmation/dialog.
␟ usage: Use for blocking confirmations, choices, or alerts over existing content.
␟ pattern: modal
␟ props: title, body_text
␟ shape: box.card

+------------------------------------------+
| Confirm                                  |
+------------------------------------------+
| Are you sure you want to quit?           |
|                                          |
|        [ Cancel ]  [ Quit ]              |
+------------------------------------------+

␟␟␟ COMPONENT: toast.inline
␟ description: Transient one- or two-line feedback (e.g. bottom-right).
␟ usage: Briefly show save confirmations or non-blocking alerts near a screen edge.
␟ props: message, variant
␟ shape: box.single

+-----------------------------------+
|  Saved.                           |
+-----------------------------------+

␟␟␟ COMPONENT: progress-bar.horizontal
␟ description: Horizontal bar with optional label/percent; used by meters.
␟ usage: Embed wherever you need a generic progress indicator, e.g. loading tasks or quests.
␟ props: value, max, label_optional

[=========>                    ] 50%

␟␟␟ COMPONENT: spinner.loading
␟ description: Animated loading indicator (cycle through frames in code).
␟ usage: Show inline while waiting on short operations such as network or disk I/O.
␟ props: (none) or style_hint, frames[]
␟ note: Implementation cycles frames e.g. | / - \ or + x.

|

---------- Typography ----------

␟␟␟ COMPONENT: typography.banner
␟ description: Multi-line ASCII-art title; structural placeholder for Figlet/Art.
␟ usage: Use on splash screens or major transitions to emphasize titles or chapter breaks.
␟ note: For production, consider generating via Figlet/Art or similar.
␟ props: text, style_hint

  ___  ____  _  __ ______
 / _ )/ __ \/ |/ // __/ /
/ _  / /_/ /    /_/ /_/ 
/____/\____/_/|_/___/___ 

---------- Game — conversation ----------

␟␟␟ COMPONENT: speech-bubble.left
␟ description: NPC/system speech; tail on left.
␟ usage: Use for NPC dialog, system narration, or radio-style chatter directed at the player.
␟ props: text, speaker_id

  +-----------------------------+
 / The guard nods. "Go north."   |
+-------------------------------+

␟␟␟ COMPONENT: speech-bubble.right
␟ description: Player speech; tail on right.
␟ usage: Use for explicit player dialog choices or typed speech in conversations.
␟ props: text

+-----------------------------+
| I'll go north.              \
+-----------------------------+

␟␟␟ COMPONENT: choice-wheel.inline
␟ description: Vertical numbered options for dialog/choice flow.
␟ usage: Present small sets of mutually exclusive options during dialog or scripted choices.
␟ props: options[]

1. Go north
2. Talk to guard
3. Take lamp

---------- Game — menus ----------

␟␟␟ COMPONENT: menu.main
␟ description: Main menu; title + list (New game, Load, Options, Quit).
␟ usage: Use on startup or return-to-menu flows for core game navigation.
␟ props: title, items[]
␟ interactive: true

+-- AskeeDS ----------------------+
|  New game                       |
|  Load game                      |
|  Options                        |
|  Quit                           |
+---------------------------------+

␟␟␟ COMPONENT: menu.pause
␟ description: Pause/options overlay; same list shape.
␟ usage: Overlay during gameplay pauses to access settings or exit options.
␟ use-case: pause | settings
␟ props: title, items[]

+-- Paused -----------------------+
|  Resume                         |
|  Settings                       |
|  Quit to menu                   |
+---------------------------------+

---------- Game — inventory/character ----------

␟␟␟ COMPONENT: inventory.grid
␟ description: Grid of slots (e.g. 2x4 or 4x3) with labels.
␟ usage: Represent backpack or equipment layouts where position matters (e.g. limited slots).
␟ props: slots[], columns

+--------+--------+--------+--------+
| slot 1 | slot 2 | slot 3 | slot 4 |
+--------+--------+--------+--------+
| slot 5 | slot 6 | slot 7 | slot 8 |
+--------+--------+--------+--------+

␟␟␟ COMPONENT: inventory.list
␟ description: List of items with optional quantity.
␟ usage: Use when item order and grouping matter more than spatial arrangement.
␟ props: items[]

- rusty sword (1)
- brass lamp (1)
- key (2)

␟␟␟ COMPONENT: character-sheet.compact
␟ description: Name plus 2–3 rows (HP, mana, status); reuses meter/label.
␟ usage: Show as a sidebar or dedicated screen summarizing the current character state.
␟ props: name, stats[]

Hero
HP:  [=========>    ] 85/100
Mana: [====          ] 20/50

␟␟␟ COMPONENT: panel.survival-status
␟ description: Compact survival panel grouping hunger, thirst, fatigue, and similar needs.
␟ usage: Use in survival-focused games to track core needs alongside HP and resources.
␟ props: needs[]
␟ color-hint: status.good/warning/danger per need band; ui.muted for labels

Survival
Hunger:   [=====               ] 30/100
Thirst:   [========            ] 55/100
Fatigue:  [===========         ] 70/100

␟␟␟ COMPONENT: tree.compact
␟ description: Indented hierarchy for skills/abilities/loadouts.
␟ usage: Show skill trees, perk lists, or nested loadouts in a text-friendly hierarchy.
␟ props: nodes[]

Combat
  > Strike
  > Block
  > Parry
Lore
  > Identify
  > Persuade

␟␟␟ COMPONENT: tree.relationships
␟ description: Indented list of NPC and faction relationships with attitudes and tags.
␟ usage: Use on a relationships or factions screen to surface social ties and standings.
␟ props: relations[]

Allies
  > Guard captain (friendly) [city, watch]
Rivals
  > Thieves' Guild (hostile) [underworld]

---------- Game — meters ----------

␟␟␟ COMPONENT: meter.resource
␟ description: Horizontal bar + label; type = health | mana | stamina.
␟ usage: Embed wherever you need a labeled resource bar, often inside sheets or HUDs.
␟ props: type, current, max
␟ color-hint: status.good/warning/danger for health band; entity for others

HP:   [=========>                    ] 85/100
Mana: [====                           ] 20/50

---------- Game — counters ----------

␟␟␟ COMPONENT: counter.ammo
␟ description: Ammo or similar resource count (current/max or bar).
␟ usage: Use in combat or ranged-weapon HUDs to show current and max ammo.
␟ props: current, max, label

Ammo: 12/24

␟␟␟ COMPONENT: counter.score
␟ description: Score or kill count.
␟ usage: Display cumulative scores, kills, or similar tallies in arcade-style modes.
␟ props: value, label

Score: 1,250   Kills: 7

␟␟␟ COMPONENT: tracker.objective
␟ description: One line per objective; unchecked/checked.
␟ usage: Show in a sidebar or overlay listing current quests or objectives with completion state.
␟ props: objectives[]

[ ] Find the key
[x] Talk to the guard
[ ] Open the door

␟␟␟ COMPONENT: tracker.clock
␟ description: Segment-style progress clock for countdowns, projects, or looming threats.
␟ usage: Pair with objectives to visualize time pressure or progress toward a key event.
␟ props: label, segments, filled, risk_level_optional
␟ color-hint: status.good/warning/danger based on proximity to completion

Quest: Find the key
[○○○○]   0 / 4

␟␟␟ COMPONENT: tracker.front
␟ description: Horizontal multi-stage track showing an advancing front or world threat.
␟ usage: Use to track escalating world states such as invasions, storms, or rumors.
␟ props: label, stages[], current_stage_index
␟ shape: stages[]: see PROP SHAPES (tracker.front uses generic { label }[]).

Invasion front:
[ Safe ]-[ Skirmishes ]-[ Occupied ]-[ Overrun ]
   ^

---------- Game — status ----------

␟␟␟ COMPONENT: status-icon.row
␟ description: Row of symbols (buffs, debuffs, alerts) with optional labels.
␟ usage: Place near HUD or sheets to summarize active buffs, debuffs, and alerts.
␟ props: icons[], labels[]

[+] [*] [!]   buff  debuff  alert

␟␟␟ COMPONENT: cooldown.badge
␟ description: Turn-based cooldown (turns left); no realtime.
␟ usage: Attach to single abilities or actions to show remaining turns before reuse.
␟ props: turns_left, label

[3]

␟␟␟ COMPONENT: cooldown.row
␟ description: Row of labeled cooldown badges for multiple abilities.
␟ usage: Show multiple abilities' cooldowns under character or combat HUD elements.
␟ props: abilities[]
␟ color-hint: ui.muted for labels; status.warning when turns_left > 0

Strike [0]  Block [2]  Dash [1]

---------- Game — notifications ----------

␟␟␟ COMPONENT: notification.achievement
␟ description: Bordered one-liner for achievements.
␟ usage: Pop up when the player meets achievement conditions or milestone goals.
␟ props: message
␟ shape: box.single

+------------------------------------------+
|  Achievement: First Steps                |
+------------------------------------------+

␟␟␟ COMPONENT: notification.loot
␟ description: Bordered one-liner for loot drops.
␟ usage: Use when the player gains items outside of the main log, such as auto-loot.
␟ props: message
␟ shape: box.single

+------------------------------------------+
|  Loot: rusty sword                       |
+------------------------------------------+

␟␟␟ COMPONENT: feedback.mixed
␟ description: Short block summarizing a mixed success (upside and downside).
␟ usage: Show after rolls or checks that succeed with complications in PbtA-style systems.
␟ props: headline, upside_text, downside_text

+------------------------------------------+
| Mixed: You dodge the blow...             |
|  Upside: You avoid the worst of it.      |
|  Downside: Your sword slips from hand.   |
-------------------------------------------+

---------- Game — screens ----------

␟␟␟ COMPONENT: screen.loading
␟ description: Centered loading message + optional tip.
␟ usage: Display while loading saves, generating worlds, or performing long operations.
␟ props: message, tip
␟ shape: box.card

+------------------------------------------+
|                                          |
|            Loading...                    |
|     Tip: Type "look" to examine.         |
|                                          |
+------------------------------------------+

␟␟␟ COMPONENT: screen.crafting
␟ description: Crafting workbench screen with input slots, output preview, and resource costs.
␟ usage: Use for dedicated crafting flows where players combine items into new outputs.
␟ props: inputs[], output_preview, success_chance_optional, resource_costs[]
␟ shape: box.composite

+------------------------------------------+
|           Crafting bench                 |
+------------------------------------------+
| Inputs:                                  |
|  [ herb ]  [ bottle ]                    |
|                                          |
| Output: healing potion                   |
| Chance: 75%                              |
|                                          |
| Costs: mana 5, time 1h                   |
+------------------------------------------+

␟␟␟ COMPONENT: screen.death
␟ description: Death screen; title + message + actions.
␟ usage: Show when the player reaches a fail state, offering respawn or exit actions.
␟ props: message, actions[]
␟ shape: box.card

+------------------------------------------+
|         You have died.                   |
|                                          |
|  The guard's blade finds you.            |
|                                          |
|    [ Respawn ]  [ Main menu ]            |
+------------------------------------------+

␟␟␟ COMPONENT: screen.tutorial
␟ description: Small bordered hint; Tutorial or Hint label.
␟ usage: Use to surface onboarding hints or contextual guidance early in play.
␟ props: title, body, dismiss_label
␟ shape: box.card

+-- Hint -------------------------+
|  Type "go north" to move.       |
|           [ OK ]                |
+---------------------------------+

---------- Game — decorative ----------

␟␟␟ COMPONENT: decoration.placeholder
␟ description: Slot for public-domain or project-owned ASCII art (fish, skull). width and height define slot size in character cells; art is supplied elsewhere and cropped or centered to fit. Use ASCII-only characters (no Unicode) per AskeeDS box-drawing/decoration policy.
␟ usage: Reserve a fixed-size area for decorative ASCII art such as logos, monsters, or scenes.
␟ note: Use public-domain or project-owned art; choose pieces from design/ascii/decoration-catalog.txt or attach specific art assets.
␟ props: width, height

    .-~~~-.
   (  o   o  )
   (   ^   )
    \|_|_/

---------- Game — consequences ----------

␟␟␟ COMPONENT: panel.consequence
␟ description: Panel listing lasting body and mental consequences with severities.
␟ usage: Use in games with long-term injuries or trauma to summarize ongoing effects.
␟ props: body_conditions[], mental_conditions[]
␟ shape: box.composite

+------------------------------------------+
| Consequences                             |
+------------------------------------------+
| Body:                                    |
|  - Scarred arm (mild)                    |
|  - Broken rib (severe)                   |
| Mental:                                  |
|  - Nightmares (moderate)                 |
|  - Shaken faith (mild)                   |
+------------------------------------------+

---------- Game — quick-select ----------

␟␟␟ COMPONENT: quick-select.radial
␟ description: Compass-style or 8-slot radial (e.g. weapon wheel).
␟ usage: Represent quick-select menus like weapon or item wheels in purely text form.
␟ props: options[], selected_id

      [1]
   [4]   [2]
      [3]
